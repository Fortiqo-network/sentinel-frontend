services:
  sentinel-frontend:
    image: ghcr.io/fortiqo-network/sentinel-frontend:latest
    container_name: sentinel-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.production

networks:
  default:
    name: sentinel-net
    external: true
